[project]
name = "knowledge-project-skills"
version = "0.1.0"
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "fpdf2>=2.7",
    "python-docx>=1.1",
    "openpyxl>=3.1",
    "pyyaml>=6.0",
]
